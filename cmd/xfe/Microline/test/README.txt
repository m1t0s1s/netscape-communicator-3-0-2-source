
Test Direction - Simple Test Programs

tfolder1  display a simple folder, sets some folder resources
tfolder2  displayed tabs on top, bottom, left, right
tgrid1    random grid testing, setting values, sanity checks
tgrid2    selection tests, creates grids with the various selection policies
tgrid3    C binding test, has references to most Grid functions, ignore
		  the resulting grid displayed on the screen
tprog1    simple progress bar test
tutil1    tests various utility functions
tutil2    tests drawing oriented strings - drag window larger twice
		  to start test
tutil3    tests drawn button orientation
